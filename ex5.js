/* 
LET

Permite que você declare variáveis limitando seu escopo no bloco, instrução, ou em uma expressão na qual ela é usada. Isso é inverso da keyword var (en-US), que define uma variável globalmente ou no escopo inteiro de uma função, independentemente do escopo de bloco.

VAR

O escopo de uma variável declarada com var é uma das seguintes sintaxes entre chaves que contém mais de perto a instrução var: 
Corpo funcional
Bloco de inicialização estática
Ou se nenhuma das opções acima se aplicar:

O módulo atual, para código executado em modo de módulo
O escopo global, para código executado em modo de script.
*/